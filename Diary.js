class Diary {

    lock = true;
    input;
    entry;

    unlock() {
        if (lock === true) {
            return;
        }
        else {
            this.addEntry(input) = entry;

            this.lookEntry(input) {
                return this.entry;
            }
        }
    }



}