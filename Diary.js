class Diary {

    lock = true;
    input;
    entry;

    addEntry() {
        if (lock === true) {
            return;
        } else {
            this.addEntry(entry) === input;
        }
    };



}

module.exports = Diary;